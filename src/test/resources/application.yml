server:
  port: 0

spring:
  application:
    name: server-eureka-test
  profiles:
    active: local

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
